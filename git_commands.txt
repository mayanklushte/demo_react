set username github

    git config --global user.name "username"

set email github
    git config --global user.email "MY_NAME@example.com"


empty repository intialize

    git init
    git remote add origin <path Name>

    git add .
    git commit -m "first commit"
    git push origin master
